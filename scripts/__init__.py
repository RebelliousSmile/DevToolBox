"""Local DevToolBox automation packages.

The explicit package marker prevents an unrelated third-party package named
``scripts`` from shadowing these modules when commands are run from the
repository root.
"""
